require "spec_helper"

RSpec.describe Ahoy::Intercom do
  it "has a version number" do
    expect(Ahoy::Intercom::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
